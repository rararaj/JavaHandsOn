package com.texasmutual.interview.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/v1")
public class UserController {




	// TODO: Add the required dependency

	/**
	*   TODO: Add new REST endpoint which converts given integer number into roman value
	*   	1) Take the integer as path parameter
	*   	2) use IntegerToRomanService to write the method implementation
	*/


	/**
	*   TODO: Add new REST endpoint to add new user to the H2 DB
	*   	1) create a userRequest class 
	*   	2) validated the userRequest with a custom class level constraint validator 
	*   		(i) controller requestBody should have three mandatory fields (name, email, contact) and one optional field contactNumber
	*   		(ii) when contact is true contactNumber(accepts only 10 digits) should mandatory, if not return 400 status with appropriate message.
	*   	3) create to a method in UserService to call the dataLayer.
	*/
	
	




}